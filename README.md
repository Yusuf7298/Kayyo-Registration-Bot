# Automations — TaskFlow Bot

Simple Telegram bot scaffold using aiogram.

Quick start

1. Install dependencies:

```bash
pip install -r requirements.txt
```

2. Run the bot:

```bash
python main.py
```

Notes
- Keep your `BOT_TOKEN` and `ADMIN_ID` in `config/settings.py` or use environment variables instead.
